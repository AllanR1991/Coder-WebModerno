const divisao = (dividendo, divisor) => {
    resultado = dividendo/divisor
    resto = dividendo%divisor
    console.log(`O resto da divisão é ${resultado} e o resto da divisão é ${resto}`)
}

divisao(11,4)